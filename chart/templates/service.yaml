apiVersion: v1
kind: Service
metadata:
  name: {{ include "mediatracker.fullname" . }}
  labels:
    {{- include "mediatracker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mediatracker.selectorLabels" . | nindent 4 }}
