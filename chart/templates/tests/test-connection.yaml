apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mediatracker.fullname" . }}-test-connection"
  labels:
    {{- include "mediatracker.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mediatracker.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
